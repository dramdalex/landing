header.header
	.header__top
		.container
			.contacts
				a.contact(href="tel:+75823658526")
					i.fa.fa-mobile.contact__icon(aria-hidden="true")
					span.contact__text 6337 3128

				a.contact(href="mail.to:info@business.co")
					i.fa.fa-envelope.contact__icon.contact__icon--email(aria-hidden="true")
					span.contact__text info@business.co

				a.facebook-icon(href="facebook.com")

	.container
		.header__bottom
			.logo: img.logo__picture(src="images/logo.png" alt="picture")
			nav.nav
				- var items = ['Home','About us','Our Approch','Our Work']
					each item in items
						if item === 'Home'
							a.nav__link.nav__link--active(href="#")=item
						else
							a.nav__link(href="#")=item
			a.button.header__button.button--red(href="#") Free Consulation
						